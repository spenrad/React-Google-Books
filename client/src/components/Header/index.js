import React from "react";
import "./style.css";

function Header () {

    
return (
    <div>
        <h1>Google Books (React)</h1>
        <h2>Search for and SAVE Books!</h2>
    </div>
    );
}

export default Header;